// Package controllers hosts all controllers of the service.
//
// Each controller responsibility is to handle business logic between the API data layer and the database data layer (no database in this case).
package controllers
